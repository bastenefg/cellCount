Use the attached CHO_viability_pipeline package to reproduce and then reuse my CHO–EBFP2 live/dead image analysis. Read README.md and inspect the code before running it. This is a scientific measurement task; preserve the raw microscopy files and report failures or uncertainties explicitly.

First reproduce the bundled reference:

1. Work in an isolated environment. Use Python 3.12.13 if available and install requirements-lock.txt. Record the actual Python and dependency versions. Do not silently upgrade packages or change the analysis code to make a check pass.
2. Run `python run_pipeline.py reference --output runs/reference_check` using a new output directory. Then run `python run_pipeline.py verify --run runs/reference_check` and `python -m unittest discover -s tests`.
3. Inspect the reference comparison and confirm that segmentation masks, per-object classifications, and results reproduce the bundled expected exports. Expected totals are S5 = 438, S6 = 526, S7 = 333; apparent viability is 39.1 ± 3.8%, and detectable blue signal among green-only objects is 80.6 ± 4.2%. Do not treat rounded agreement with those two percentages as sufficient evidence of reproduction. If the reference checks fail, diagnose and explain the discrepancy before analyzing new images; do not retune parameters to recover the expected percentages.

Then analyze the new images I provide:

4. Create an explicit samples.csv with columns `image_id,replicate_id,green,red,ebfp`, one row per corresponding live/dead field, with EBFP optional. Leave an unacquired EBFP cell blank or omit that column entirely; do not substitute a zero-filled image. A provided EBFP image with no signal must still be analyzed. A specified file that cannot be found is an error to resolve, not a channel to silently omit. Use paths relative to that CSV. Ask me for missing sample-to-gel assignments rather than inventing independent replicates. Multiple nonoverlapping fields of the same gel must have the same replicate_id. Keep separate conditions/timepoints in separate runs because this manifest has no condition grouping.
5. Check image dimensions, bit depth, scalar channel storage, channel correspondence, field calibration, and acquisition comparability. The reference preset is for 1024 × 1024 uint8 images spanning 2327.27 × 2327.27 µm. If settings differ materially, explain which parameters require review and prepare a separately named configuration from controls and image-quality evidence. Do not normalize each image automatically, convert a stack into a projection, rescale bit depth silently, register channels silently, or reuse thresholds without assessing comparability.
6. Freeze the manifest and configuration before the final run. The EBFP multiple-testing family consists of scorable objects across the entire run; adding images can change borderline q-value classifications. Do not combine or exclude images after seeing the results without recording a new run and the reason.
7. Run `python run_pipeline.py analyze --manifest samples.csv --config configs/reference_48h.json --output runs/new_experiment --extended-qc`, substituting a reviewed, separately named configuration if necessary. Use a new output path if one already exists. Run `python run_pipeline.py verify --run runs/new_experiment` afterward.
8. Review numbered detection overlays, the object audit flags, missing/unscorable EBFP measurements, and threshold sensitivity. Flag likely missed cells, debris, merged objects, and ambiguous double positives. Do not edit individual classifications or remove fields silently. Propose any method changes as a separate, documented version and retain the original run.
9. Report per-image and per-replicate results. Pool counts within each gel, then calculate an unweighted mean ± sample SD across independent gels. Keep double-positive objects separate in the tables and apply the preset's red-priority viability definition. Report EBFP-positive numerator, scorable denominator, channel-not-provided counts, and other unscorable counts. For live/dead-only data, report EBFP as not measured and produce the viability-only figure. For mixed acquisitions, make EBFP coverage explicit. Do not equate detectable blue fluorescence with ongoing protein production or a negative blue call with loss of expression.
10. Deliver the figure PNG/SVG, summary tables, complete run folder, manifest, effective configuration, version information, checksums, and the code package. State whether the reference reproduction passed and what remains uncertain biologically. Use original intensity data for measurements and only documented fixed linear scaling for image display. Never generate, retouch, or replace microscopy content.

If I have supplied only this package and no new images, complete the reference reproduction and explain exactly how to add my next dataset. Preserve all existing runs and input TIFFs.
